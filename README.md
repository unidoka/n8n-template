# Telegram SMM automatization bot

## How to run

1) Add XRAY config to the xray_config.json (if you need it)

2) Add n8n credentials to the n8n-backups/credentials.json

3) Run

```bash
docker compose up
```